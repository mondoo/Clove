<UserControl x:Class="Garlic.Bulb.DirectoryView"
			 x:Name="this"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" d:DesignWidth="267.396" d:DesignHeight="155.156">
	<ItemsControl ItemsSource="{Binding AllItems}" 
				  ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
				  AllowDrop="True"
				  Drop="ItemsControl_Drop"
				  Background="Orange"> <!-- TODO: Can only drag and drop with a background. This is likely a sizing issue. Needs investigation -->
		<ItemsControl.ItemsPanel>
			<ItemsPanelTemplate>
				<WrapPanel Orientation="Horizontal" ItemWidth="50"/>
			</ItemsPanelTemplate>
		</ItemsControl.ItemsPanel>

		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<Button Background="Transparent"
						BorderBrush="Transparent">
					<Button.InputBindings>
						<MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenCommand}"/>
					</Button.InputBindings>

					<StackPanel>
						<Image HorizontalAlignment="Center">
							<Image.Style>
								<Style TargetType="{x:Type Image}">
									<Style.Triggers>
										<DataTrigger Binding="{Binding Type}" Value="0">
											<Setter Property="Source" Value="/resource/FileExplorer/png/001-folder-closed-black-shape.png"/>
										</DataTrigger>
										<DataTrigger Binding="{Binding Type}" Value="1">
											<Setter Property="Source" Value="/resource/FileExplorer/png/002-file.png"/>
										</DataTrigger>
									</Style.Triggers>
								</Style>
							</Image.Style>
						</Image>
						<TextBlock Text="{Binding Name}"
								   HorizontalAlignment="Center"/>
					</StackPanel>
				</Button>
			</DataTemplate>
		</ItemsControl.ItemTemplate>
	</ItemsControl>
</UserControl>