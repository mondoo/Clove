#pragma once

namespace clv::gfx {
    /**
     * @brief A Shader represents a small piece of code that runs on the GPU
     */
    class Shader {
        //FUNCTIONS
    public:
        virtual ~Shader() = default;
    };
}