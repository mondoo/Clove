#pragma once

namespace garlic::clove {
    /**
     * @brief Creates a file system that will not change even on different
     * platforms / install locations.
     */
    class VirtualFileSystem {
    };
}