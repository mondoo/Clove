<Window x:Class="Bulb.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="1" Grid.Row="0" x:Name="RenderArea" Margin="5 0 0 0" BorderBrush="Black" BorderThickness="1" VerticalAlignment="Stretch"/>

        <Grid Margin="0 0 5 0">
            <Button x:Name="AddEntityButton" VerticalAlignment="Top" Content="Add Entity" Click="AddEntityButton_Click"/>
        </Grid>

    </Grid>
</Window>
